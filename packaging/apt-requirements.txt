# WhatsApp for Linux - Debian/Ubuntu sistem bagimliliklari.
# scripts/install.sh bu dosyayi okuyup kurar. Elle kurmak icin:
#   sudo apt install -y $(grep -v '^#' packaging/apt-requirements.txt | grep -v '^$')

# Webview ve pencere sistemi
libwebkit2gtk-4.1-dev
libgtk-3-dev

# Sistem tepsisi (tray) ikonu
libayatana-appindicator3-dev

# Ikon donusumu (Tauri bundle asamasinda gerekli)
librsvg2-dev

# Rust derleyicisinin ihtiyaci olan araclar
build-essential
curl
wget
file
libssl-dev
pkg-config
